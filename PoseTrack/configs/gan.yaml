# configs/gan.yaml
# GAN Refinery Model Specific Settings

model:
  seq_len: 60
  batch_size: 32
  pretrain_epochs: 20
  gan_epochs: 60
  lr_g: 0.0001
  lr_d: 0.00004
  lambda_recon: 10.0
  lambda_adv: 1.0
  lambda_smooth: 5.0
